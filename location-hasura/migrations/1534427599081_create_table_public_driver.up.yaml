- args:
    sql: CREATE TABLE public."driver"("id" serial NOT NULL, "name" text NOT NULL,
      "vehicle_number" text NOT NULL UNIQUE, PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: driver
    schema: public
  type: add_existing_table_or_view
